import React from 'react'

type Props = {
  onCancel: () => void
  onSubmit: (text: string, file?: string) => void
}

const StoryFormImage = (props: Props) => {
  return <div>StoryFormImage</div>
}

export default StoryFormImage
